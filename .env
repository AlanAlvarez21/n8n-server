# This file contains the environment variables for the n8n and PostgreSQL setup
# Please change these values for security reasons before starting the services

# PostgreSQL Root User Credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres_password

# PostgreSQL Database Name
POSTGRES_DB=n8n

# PostgreSQL Non-Root User Credentials (used by n8n to connect)
POSTGRES_NON_ROOT_USER=n8n
POSTGRES_NON_ROOT_PASSWORD=n8n_password